# Core Dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Utilities
tqdm>=4.65.0
faker>=20.0.0

# Optional: For training BERT model
datasets>=2.14.0
seqeval>=1.2.2
scikit-learn>=1.3.0

# Optional: For environment variable management
python-dotenv>=1.0.0

# Note: Install PyTorch with CUDA support for GPU acceleration:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# For CPU-only installation:
# pip install torch --index-url https://download.pytorch.org/whl/cpu